#include <iostream>
#include "Chess.h"
#include <vector>
#include <algorithm>

using namespace Chess;

int main(){
    ChessBoard temp;
    temp.StartGame();

    std::vector<int> v = {1, 2, 3, 4, 5};

}